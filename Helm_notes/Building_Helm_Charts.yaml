

# version of Helm v1 for Helm 2, v2 for Helm 3
apiVersion: v2
# name of chart
name: guestbook
description: A Helm chart for K8s
# useful for searching in repository
keywords:
  - demo
# helper files that have some logical function to create the chart
# maybe you need an abstract library of functions shared or used
# you could tag your type as attribute, this is a new feature
# not a library exists yet
type: application
# version of application you install with helm, could be any number or string
appVersion: "2.0"
# chart version of chart major: 1, minor: 1, patch: 0
version: 1.1.0
